import { combineReducers } from 'redux';

const initState = {
  loggedIn: false,
  user: null,
  mealPlan: null,
};

const UserReducer = (state = initState, { payload }) => {
  /* switch (type) {
    case 
  } */
};
